{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install --force",
  "framework": "nextjs",
  "regions": ["sfo1"],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}
